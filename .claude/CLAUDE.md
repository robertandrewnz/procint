# Procint — Claude Code Instructions

## Git workflow

Always commit directly to main and push. Never create pull requests or branches. Every change should be committed and pushed to main immediately upon completion so Railway auto-deploys.
