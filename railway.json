{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn portal:app --workers 2 --bind 0.0.0.0:$PORT --timeout 120 --log-level debug",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
